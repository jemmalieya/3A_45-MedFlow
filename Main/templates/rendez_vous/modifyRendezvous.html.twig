{% extends 'base.html.twig' %}
{% set patientId = app.session.get('patient_id') %}

{% block body %}
  <style>
    :root {
      --mf-vivid-blue:#003399;
      --mf-dark-turquoise:#0097B2;
      --mf-vivid-turquoise:#00BFA6;
      --mf-pastel-blue:#6797FF;
      --mf-pastel-azure:#8FD5FF;
      --mf-light-blue:#B3D5FF;
      --mf-white:#FFFFFF;
      --mf-black:#000000;
      --mf-vivid-red:#BC0B09;
      --mf-text:#101828;
      --mf-muted:#6c757d;
      --shadow-sm:0 10px 26px rgba(16,24,40,.06);
      --shadow-md:0 16px 40px rgba(16,24,40,.08);
      --shadow-lg:0 24px 56px rgba(16,24,40,.12);
    }
    .appointment-page {
      min-height: 100vh;
      background: var(--mf-white);
      padding: 2rem 0 4rem;
      font-family: 'Inter', 'Segoe UI', Arial, sans-serif;
    }
    .mf-rect-hero {
      border-radius: 24px;
      background: var(--mf-dark-turquoise);
      box-shadow: var(--shadow-md);
      border: 1px solid rgba(0,0,0,.08);
      padding: 40px 24px;
      text-align: center;
      margin-bottom: 2.5rem;
    }
    .mf-rect-title {
      font-size: 2.25rem;
      font-weight: 900;
      color: var(--mf-white);
      margin-bottom: 0.5rem;
      letter-spacing: -.02em;
    }
    .mf-rect-sub {
      color: var(--mf-pastel-azure);
      font-size: 1.1rem;
      font-weight: 500;
      opacity: 0.95;
    }
    .mf-card {
      background: var(--mf-light-blue);
      border-radius: 24px;
      box-shadow: var(--shadow-md);
      border: 1.5px solid var(--mf-dark-turquoise);
      padding: 3rem 3.5rem 2.5rem 3.5rem;
      margin-bottom: 2.5rem;
      width: 100%;
      max-width: 100%;
      box-sizing: border-box;
    }
    .mf-card .form-label {
      font-weight: 600;
      color: var(--mf-dark-turquoise);
      margin-bottom: 0.5rem;
    }
    .mf-card .form-control, .mf-card .form-select {
      border: 2px solid var(--mf-dark-turquoise);
      border-radius: 12px;
      padding: 0.75rem 1rem;
      font-size: 1rem;
      transition: border-color 0.2s, box-shadow 0.2s;
    }
    .mf-card .form-control:focus, .mf-card .form-select:focus {
      border-color: var(--mf-dark-turquoise);
      box-shadow: 0 0 0 4px #0097b233;
      outline: none;
    }
    .mf-card .form-control[readonly],
    .mf-card .form-control:disabled,
    .mf-card .form-control.bg-muted {
      background: var(--mf-pastel-azure);
      color: var(--mf-dark-turquoise);
    }
    .btn-mf {
      background: var(--mf-dark-turquoise);
      border-color: var(--mf-dark-turquoise);
      color: #fff !important;
      border-radius: 999px !important;
      padding: 0.5rem 1.3rem;
      font-weight: 700;
      font-size: 1.05rem;
      letter-spacing: 0.02em;
      transition: transform 0.2s, box-shadow 0.2s;
      box-shadow: 0 2px 8px #0097B222;
    }
    .btn-mf:hover {
      background: #007E94;
      border-color: #007E94;
      color: #fff !important;
      transform: translateY(-2px);
      box-shadow: 0 12px 28px rgba(0,151,178,0.25);
    }
    .btn-mf-outline {
      background: #fff;
      border: 2px solid var(--mf-dark-turquoise);
      color: var(--mf-dark-turquoise);
      border-radius: 999px !important;
      padding: 0.5rem 1.3rem;
      font-weight: 700;
      font-size: 1.05rem;
      letter-spacing: 0.02em;
      transition: background 0.2s, color 0.2s, box-shadow 0.2s;
      box-shadow: 0 2px 8px #0097B222;
    }
    .btn-mf-outline:hover {
      background: rgba(0,151,178,.08);
      border-color: var(--mf-dark-turquoise);
      color: var(--mf-dark-turquoise);
    }
    .btn-pill {
      border-radius: 999px !important;
      padding-left: 14px !important;
      padding-right: 14px !important;
    }
    .alert-mf {
      border: none;
      border-radius: 14px;
      padding: 1rem 1.25rem;
      font-weight: 500;
    }
    .alert-mf.alert-danger { background: linear-gradient(135deg, #fef2f2 0%, #fee2e2 100%); color: #991b1b; }
    .alert-mf.alert-success { background: linear-gradient(135deg, #dbeafe 0%, #bae6fd 100%); color: #0097B2; }
    .alert-mf.alert-info { background: linear-gradient(135deg, #e0f2fe 0%, #bae6fd 100%); color: #0097B2; }
    .mf-card .form-error,
    .mf-card .form-errors,
    .mf-card ul.form-errors,
    .mf-card .invalid-feedback,
    .mf-card .form-error-message,
    .mf-card .mb-4 ul,
    .mf-card .mb-4 .form-errors,
    #motif-error ul {
      color: #dc3545 !important;
      font-size: 0.98em;
      font-weight: 500;
      margin-top: 0.25rem;
      margin-bottom: 0.25rem;
      list-style: disc inside;
    }
    #appointment-form {
      padding-top: 100px;
    }
    @media (max-width: 768px) {
      #appointment-form {
        padding-top: 80px;
      }
      .mf-rect-title {
        font-size: 1.75rem;
      }
    }
    #fullcalendar {
      background: #f8fafc;
      border-radius: 22px;
      box-shadow: 0 8px 32px rgba(0,151,178,0.13), 0 2px 8px rgba(0,151,178,0.08);
      padding: 2rem 2rem 1.5rem 2rem;
      margin-bottom: 2rem;
      border: 2px solid #bae6fd;
    }
    input[type="datetime-local"], .js-datepicker, .flatpickr-input {
      display: none !important;
    }
  </style>

  <main class="main appointment-page">
    <section id="appointment-form" class="section py-5">
      <div class="container">
        <div class="row justify-content-center">
          <div class="col-12">
            <div class="mf-rect-hero mb-4">
              <div class="mf-rect-inner">
                <h1 class="mf-rect-title">📅 Modifier rendez-vous</h1>
                <p class="mf-rect-sub">Modifier votre rendez-vous existant.</p>
              </div>
            </div>
            {# Flash Messages #}
            {% for label, messages in app.flashes %}
              {% for message in messages %}
                <div class="alert alert-mf alert-{{ label == 'error' ? 'danger' : 'success' }}">{{ message }}</div>
              {% endfor %}
            {% endfor %}
            <div class="mf-card">
              {{ form_start(form, {'attr': {'novalidate': 'novalidate', 'id': 'appointment-form-main'}}) }}
              <div class="mb-4">
                <div id="fullcalendar"></div>
                <div id="selected-date-label" style="margin-top:1rem;font-weight:bold;color:var(--mf-dark-turquoise);"></div>
                <input type="hidden" id="calendar-datetime" name="{{ form.datetime.vars.full_name }}" value="{{ form.datetime.vars.value|default('') }}">
                {{ form_errors(form.datetime) }}
              </div>
              {{ form_row(form.mode) }}
              <div class="mb-3">
                {{ form_label(form.motif) }}
                {{ form_widget(form.motif, {'attr': {'class': 'form-control', 'aria-describedby': 'motif-error'}}) }}
                <div id="motif-error">{{ form_errors(form.motif) }}</div>
              </div>
              {{ form_row(form._token) }}
              <div class="d-grid mt-4">
                <button type="submit" class="btn btn-mf btn-pill">Confirmer</button>
              </div>
              <div class="d-grid mt-4">
                <a href="{{ path('rendezvous_list') }}" class="btn btn-mf-outline btn-pill">Annuler</a>
              </div>
              {{ form_end(form, {'render_rest': false}) }}
              <div class="row g-3 mb-4">
                <div class="col-md-6">
                  <label class="form-label">Patient</label>
                  <div class="form-control bg-muted" style="pointer-events: none;">
                    <strong>
                      {% if form.vars.data.patient and form.vars.data.patient.id == patientId %}
                        Moi ({{ form.vars.data.patient.nom ~ ' ' ~ form.vars.data.patient.prenom }})
                      {% else %}
                        {{ form.vars.data.patient ? form.vars.data.patient.nom ~ ' ' ~ form.vars.data.patient.prenom : 'Non défini' }}
                      {% endif %}
                    </strong>
                  </div>
                </div>
                <div class="col-md-6">
                  <label class="form-label">Personnel médical</label>
                  <div class="form-control bg-muted" style="pointer-events: none;">
                    <strong>{{ form.vars.data.staff ? form.vars.data.staff.nom ~ ' ' ~ form.vars.data.staff.prenom : 'Non défini' }}</strong>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>
  </main>
{% endblock %}

{% block javascripts %}
  {{ parent() }}
  <link href="https://cdn.jsdelivr.net/npm/@fullcalendar/core@6.1.11/main.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/@fullcalendar/daygrid@6.1.11/main.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/@fullcalendar/timegrid@6.1.11/main.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/@fullcalendar/core@6.1.11/index.global.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@fullcalendar/daygrid@6.1.11/index.global.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@fullcalendar/timegrid@6.1.11/index.global.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@fullcalendar/interaction@6.1.11/index.global.min.js"></script>
  <script>
    document.addEventListener('DOMContentLoaded', function() {
      var form = document.getElementById('appointment-form-main');
      var input = document.getElementById('calendar-datetime');
      var dateLabel = document.getElementById('selected-date-label');
      var datetimeChanged = false;

      if (form) {
        form.addEventListener('submit', function(e) {
          // Only update hidden input if user changed datetime
          if (datetimeChanged && dateLabel && input && dateLabel.textContent.trim() !== '') {
            var labelVal = dateLabel.textContent.trim();
            var match = labelVal.match(/(\d{4}-\d{2}-\d{2}) (\d{2}):(\d{2}):\d{2}/);
            if (match) {
              input.value = match[1] + 'T' + match[2] + ':' + match[3];
            }
          }
          // DEBUG: Log to console
          console.log('Form submit: hidden input value =', input ? input.value : 'null');
        });
      }

      var calendarEl = document.getElementById('fullcalendar');
      var initialDate = input.value ? new Date(input.value) : new Date();

      var doctorId = {{ form.vars.data.staff ? form.vars.data.staff.id : 'null' }};
      var calendar = new FullCalendar.Calendar(calendarEl, {
        initialView: 'timeGridWeek',
        nowIndicator: true,
        selectable: true,
        allDaySlot: false,
        weekends: false,
        headerToolbar: {
          left: 'prev,next today',
          center: 'title',
          right: 'dayGridMonth,timeGridWeek,timeGridDay'
        },
        height: 500,
        slotMinTime: '08:00:00',
        slotMaxTime: '17:00:00',
        slotDuration: '01:00:00',
        snapDuration: '01:00:00',
        events: doctorId ? '/doctor/appointments/' + doctorId : [],
        eventColor: '#dc3545', // Red
        eventRender: function(info) {
          info.el.style.backgroundColor = '#dc3545';
          info.el.style.borderColor = '#dc3545';
          info.el.style.color = '#fff';
        },
        eventContent: function(arg) {
          return { html: '<div style="color:#fff;font-weight:bold">Occupé(e)</div>' };
        },
        businessHours: {
          daysOfWeek: [1, 2, 3, 4, 5], // Monday to Friday
          startTime: '08:00',
          endTime: '17:00'
        },
        selectConstraint: 'businessHours',
        eventOverlap: false,
        selectAllow: function(selectInfo) {
          var events = calendar.getEvents();
          for (var i = 0; i < events.length; i++) {
            if (
              selectInfo.start < events[i].end &&
              selectInfo.end > events[i].start
            ) {
              return false;
            }
          }
          var diff = (selectInfo.end - selectInfo.start) / (1000 * 60 * 60);
          return diff === 1;
        },
        select: function(info) {
          // Mark that datetime was changed
          datetimeChanged = true;
          // Set the hidden input value to the selected datetime in 'YYYY-MM-DDTHH:mm' format for Symfony
          if (input) {
            var dt = new Date(info.startStr);
            var year = dt.getFullYear();
            var month = ('0' + (dt.getMonth() + 1)).slice(-2);
            var day = ('0' + dt.getDate()).slice(-2);
            var hour = ('0' + dt.getHours()).slice(-2);
            var minute = ('0' + dt.getMinutes()).slice(-2);
            input.value = year + '-' + month + '-' + day + 'T' + hour + ':' + minute;
          }
          // Show selected date/time below the calendar in 'YYYY-MM-DD HH:mm:00' format
          if (dateLabel) {
            var dt = new Date(info.startStr);
            var year = dt.getFullYear();
            var month = ('0' + (dt.getMonth() + 1)).slice(-2);
            var day = ('0' + dt.getDate()).slice(-2);
            var hour = ('0' + dt.getHours()).slice(-2);
            var minute = ('0' + dt.getMinutes()).slice(-2);
            dateLabel.textContent = year + '-' + month + '-' + day + ' ' + hour + ':' + minute + ':00';
          }
          var selectedEls = document.querySelectorAll('.fc-slot-selected');
          selectedEls.forEach(function(el) { el.classList.remove('fc-slot-selected'); });
          var slotSelector = '[data-time="' + info.startStr.substring(11,16) + '"]';
          var dayCol = document.querySelector('[data-date="' + info.startStr.substring(0,10) + '"]');
          if(dayCol) {
            var slot = dayCol.querySelector(slotSelector);
            if(slot) slot.classList.add('fc-slot-selected');
          }
        },
      });
      calendar.render();
    });
  </script>
{% endblock %}