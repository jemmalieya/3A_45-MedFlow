<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Facture #{{ commande.idCommande }}</title>
    <style>
        body { font-family: DejaVu Sans, sans-serif; font-size: 12px; }
        table { width: 100%; border-collapse: collapse; margin-top: 15px; }
        th, td { border: 1px solid #ddd; padding: 8px; }
        th { background: #f5f5f5; }
        .right { text-align: right; }
    </style>
</head>
<body>

<h2>Facture - Commande #{{ commande.idCommande }}</h2>
<p>Date : {{ commande.dateCreationCommande|date('d/m/Y H:i') }}</p>
<p>Statut : {{ commande.statutCommande }}</p>

<table>
    <thead>
        <tr>
            <th>Produit</th>
            <th class="right">Prix</th>
            <th class="right">Quantité</th>
            <th class="right">Sous-total</th>
        </tr>
    </thead>
    <tbody>
    {% for ligne in commande.ligneCommandes %}
        <tr>
            <td>{{ ligne.produit.nomProduit }}</td>
            <td class="right">{{ ligne.produit.prixProduit|number_format(2, '.', '') }} Dt</td>
            <td class="right">{{ ligne.quantite_commandee }}</td>
            <td class="right">{{ (ligne.produit.prixProduit * ligne.quantite_commandee)|number_format(2, '.', '') }} Dt</td>
        </tr>
    {% endfor %}
    </tbody>
    <tfoot>
        <tr>
            <th colspan="3" class="right">TOTAL</th>
            <th class="right">{{ commande.montantTotal|number_format(2, '.', '') }} Dt</th>
        </tr>
    </tfoot>
</table>

</body>
</html>
