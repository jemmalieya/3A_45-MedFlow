<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Facture #{{ commande.idCommande }}</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body { 
            font-family: 'DejaVu Sans', Arial, sans-serif;
            font-size: 11px;
            color: #333;
            line-height: 1.6;
            padding: 40px;
            background: #f8f9fa;
        }
        
        .container {
            max-width: 800px;
            margin: 0 auto;
            background: white;
            padding: 40px;
            box-shadow: 0 0 20px rgba(0,0,0,0.1);
        }
        
        .header {
            border-bottom: 3px solid #2c3e50;
            padding-bottom: 20px;
            margin-bottom: 30px;
        }
        
        h2 {
            color: #2c3e50;
            font-size: 28px;
            font-weight: 600;
            margin-bottom: 10px;
        }
        
        .facture-info {
            display: flex;
            justify-content: space-between;
            margin-bottom: 30px;
            background: #f8f9fa;
            padding: 20px;
            border-radius: 8px;
        }
        
        .info-block {
            flex: 1;
        }
        
        .info-block p {
            margin: 5px 0;
            font-size: 12px;
        }
        
        .info-label {
            font-weight: 600;
            color: #2c3e50;
            display: inline-block;
            min-width: 80px;
        }
        
        .statut {
            display: inline-block;
            padding: 4px 12px;
            border-radius: 20px;
            font-size: 11px;
            font-weight: 600;
            background: #27ae60;
            color: white;
        }
        
        table { 
            width: 100%;
            border-collapse: collapse;
            margin: 30px 0;
            font-size: 11px;
        }
        
        thead {
            background: linear-gradient(135deg, #2c3e50 0%, #34495e 100%);
            color: white;
        }
        
        th {
            padding: 14px 12px;
            text-align: left;
            font-weight: 600;
            text-transform: uppercase;
            font-size: 10px;
            letter-spacing: 0.5px;
        }
        
        td {
            padding: 12px;
            border-bottom: 1px solid #ecf0f1;
        }
        
        tbody tr {
            transition: background 0.2s;
        }
        
        tbody tr:hover {
            background: #f8f9fa;
        }
        
        tbody tr:last-child td {
            border-bottom: 2px solid #2c3e50;
        }
        
        .right { 
            text-align: right;
        }
        
        tfoot {
            background: #ecf0f1;
            font-weight: 600;
        }
        
        tfoot tr {
            border-top: 3px solid #2c3e50;
        }
        
        tfoot th {
            background: transparent;
            color: #2c3e50;
            padding: 16px 12px;
            font-size: 14px;
            text-transform: none;
        }
        
        .total-amount {
            color: #27ae60;
            font-size: 16px;
            font-weight: 700;
        }
        
        .footer {
            margin-top: 40px;
            padding-top: 20px;
            border-top: 2px solid #ecf0f1;
            text-align: center;
            color: #7f8c8d;
            font-size: 10px;
        }
        
        .prix-unitaire {
            color: #7f8c8d;
            font-size: 10px;
        }
        
        .produit-nom {
            font-weight: 500;
            color: #2c3e50;
        }
    </style>
</head>
<body>

<div class="container">
    <div class="header">
        <h2>FACTURE</h2>
        <p style="color: #7f8c8d; font-size: 13px;">Commande #{{ commande.idCommande }}</p>
    </div>
    
    <div class="facture-info">
        <div class="info-block">
            <p><span class="info-label">Date :</span> {{ commande.dateCreationCommande|date('d/m/Y à H:i') }}</p>
            <p><span class="info-label">Statut :</span> <span class="statut">{{ commande.statutCommande }}</span></p>
        </div>
    </div>

    <table>
        <thead>
            <tr>
                <th>Produit</th>
                <th class="right">Prix unitaire</th>
                <th class="right">Quantité</th>
                <th class="right">Montant</th>
            </tr>
        </thead>
        <tbody>
        {% for ligne in commande.ligneCommandes %}
            <tr>
                <td><span class="produit-nom">{{ ligne.produit.nomProduit }}</span></td>
                <td class="right prix-unitaire">{{ ligne.produit.prixProduit|number_format(2, ',', ' ') }} Dt</td>
                <td class="right">{{ ligne.quantite_commandee }}</td>
                <td class="right">{{ (ligne.produit.prixProduit * ligne.quantite_commandee)|number_format(2, ',', ' ') }} Dt</td>
            </tr>
        {% endfor %}
        </tbody>
        <tfoot>
            <tr>
                <th colspan="3" class="right">TOTAL</th>
                <th class="right total-amount">{{ commande.montantTotal|number_format(2, ',', ' ') }} Dt</th>
            </tr>
        </tfoot>
    </table>
    
    <div class="footer">
        <p>Merci pour votre confiance</p>
    </div>
</div>

</body>
</html>