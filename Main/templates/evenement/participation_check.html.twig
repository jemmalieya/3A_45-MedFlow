{% extends 'base.html.twig' %}

{% block title %}Ticket Participation{% endblock %}

{% block body %}
<section class="section" style="padding-top:120px;">
  <div class="container">
    <div class="card border-0 shadow-sm p-4 rounded-4">
      <h3 class="fw-bold mb-2">🎫 Ticket Participation</h3>

      <p class="mb-1"><b>Événement :</b> {{ evenement.titreEvent }}</p>
      <p class="mb-1"><b>Participant :</b> {{ demande.nom }}</p>
      <p class="mb-1"><b>Email :</b> {{ demande.email }}</p>

      {% set s = (demande.status ?? 'pending') %}
      <p class="mt-3">
        <b>Statut :</b>
        {% if s == 'accepted' %}
          <span class="badge bg-success">ACCEPTÉE</span>
        {% elseif s == 'refused' %}
          <span class="badge bg-danger">REFUSÉE</span>
        {% else %}
          <span class="badge bg-warning text-dark">EN ATTENTE</span>
        {% endif %}
      </p>

      {% if demande.note is defined and demande.note %}
        <div class="alert alert-info mt-3">
          <b>Note admin :</b> {{ demande.note }}
        </div>
      {% endif %}

      <a href="{{ path('app_evenements') }}" class="btn btn-outline-secondary mt-3">
        Retour aux événements
      </a>
    </div>
  </div>
</section>
{% endblock %}
