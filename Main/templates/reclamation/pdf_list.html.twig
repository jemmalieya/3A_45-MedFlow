<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <style>
    body { font-family: DejaVu Sans, sans-serif; font-size: 12px; color:#111; }
    h1 { text-align:center; margin:0 0 10px; }
    .meta { font-size: 10px; color:#666; display:flex; justify-content:space-between; margin-bottom:12px; }
    table { width:100%; border-collapse: collapse; }
    th, td { border: 1px solid #ddd; padding: 8px; vertical-align: top; }
    th { background:#f2f2f2; }
    .small { font-size: 10px; color:#666; }
  </style>
</head>
<body>

  <h1>Liste des réclamations</h1>

  <div class="meta">
    <div>
      <div><b>Recherche:</b> {{ q ? q : '—' }}</div>
      <div class="small"><b>Tri:</b> {{ sort }} ({{ dir }})</div>
    </div>
    <div class="small">Généré le {{ generatedAt|date('d/m/Y H:i') }}</div>
  </div>

  <table>
    <thead>
      <tr>
        <th>Réf</th>
        <th>Contenu</th>
        <th>Type</th>
        <th>Statut</th>
        <th>Date</th>
      </tr>
    </thead>
    <tbody>
      {% for r in reclamations %}
        <tr>
          <td>{{ r.referenceReclamation }}</td>
          <td>
            <div><b>{{ r.contenu }}</b></div>
            <div class="small">{{ r.description }}</div>
          </td>
          <td>{{ r.type }}</td>
          <td>{{ r.statutReclamation }}</td>
          <td>{{ r.dateCreationR ? r.dateCreationR|date('d/m/Y H:i') : '-' }}</td>
        </tr>
      {% else %}
        <tr>
          <td colspan="5" style="text-align:center;">Aucune réclamation</td>
        </tr>
      {% endfor %}
    </tbody>
  </table>

</body>
</html>
